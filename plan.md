
## نظرة عامة

تحويل النظام من تخزين محلي إلى نظام احترافي متكامل بقاعدة بيانات مشتركة، صوت بشري حقيقي، صور فعلية (مش إيموجي)، ولوحة تحكم محمية بتسجيل دخول.

نظرًا لحجم الشغل، هينفذ على 3 مراحل (كل مرحلة في رد منفصل عشان نتأكد إن كل حاجة شغالة قبل ما نبني فوقها).

---

## المرحلة 1 — البنية التحتية (هذا الرد)

### 1. تفعيل Lovable Cloud
- قاعدة بيانات مشتركة لكل المستخدمين
- نظام تسجيل دخول (إيميل + كلمة سر) للأدمن فقط

### 2. جداول قاعدة البيانات
```text
categories (id, slug, title_ar, emoji, gradient, bg, level, sort_order, visible)
items (id, category_id, ar, en, image_url, sound_text, hint, sort_order, visible)
user_roles (user_id, role: 'admin'|'user')  ← منفصل عن profiles لمنع تسلل الصلاحيات
```
- RLS: قراءة عامة للجميع، كتابة للأدمن فقط (عبر `has_role()`)
- Seed: نقل كل الأقسام والعناصر الحالية تلقائيًا

### 3. لوحة التحكم
- إخفاء `/admin` من الصفحة الرئيسية تمامًا (مفيش زرار ظاهر)
- الوصول عن طريق رابط مباشر `/admin` فقط
- صفحة `/admin/login` بتسجيل دخول حقيقي
- لو مش أدمن → مفيش وصول
- التحديثات تتحفظ في DB → كل المستخدمين يشوفوها فورًا

### 4. ربط ElevenLabs
- تفعيل connector (هيطلب منك API key)
- Server function: `/api/tts` ترجع صوت MP3
- صوت عربي طبيعي (Laila / Sarah voice)
- Cache للأصوات في الذاكرة عشان نوفر استهلاك

---

## المرحلة 2 — الصور الحقيقية والمحتوى الموسع (الرد التالي)

### الصور الحقيقية
نظرًا لاستحالة توليد 200×عدد الأقسام صورة في مرة واحدة، الحل العملي:
- استخدام صور حقيقية من **Unsplash Source API** (مجاني، بدون API key)
- صيغة: `https://source.unsplash.com/400x400/?cow,animal` (نفس الصورة بترجع لنفس الكلمة → ثبات)
- فلتر CSS `grayscale(100%)` للستايل الحقيقي اللي طلبته
- fallback لإيموجي لو الصورة ما حملتش

### توسيع المحتوى لـ 200 عنصر/قسم
- الحيوانات: 200 (المزرعة، البرية، الطيور، البحر، الحشرات، الزواحف)
- الفواكه: 80+ نوع حقيقي
- الخضروات: 80+ نوع
- الحروف العربية: 28 (كاملة)
- الأرقام: 1-100
- الألوان، المواصلات، الأكل، الشرب، العائلة، المشاعر، الجو، الوقت، الأفعال، المدرسة، الأماكن، المهن، الآداب، أعضاء الجسم، الملابس، الأدوات
- مخزن في DB → الأدمن يقدر يضيف/يعدل/يحذف أي حاجة

### خلفيات احترافية لكل قسم
- gradient + صورة موضوعية (farm scene, ocean, jungle...)

---

## المرحلة 3 — الصقل النهائي (رد ثالث)

- تحسين الـ UI للموبايل (Android specifically)
- تحسين النطق: تنظيف النصوص قبل إرسالها لـ ElevenLabs
- إضافة شاشة "إدارة المحتوى" متطورة في لوحة التحكم: drag & drop للترتيب، رفع صور مخصصة، معاينة فورية
- تحسينات الأداء: lazy loading للصور، prefetch للأصوات الشائعة

---

## تفاصيل تقنية

- **Stack**: TanStack Start + Supabase (Lovable Cloud) + ElevenLabs
- **Auth**: Supabase Auth بإيميل/كلمة سر، جدول `user_roles` منفصل، فحص الدور بـ security definer function
- **TTS**: ElevenLabs `eleven_multilingual_v2` بصوت `Sarah` (EXAVITQu4vr4xnSDxMaL)، sample يتولد على الـ server وكاش في memory
- **الصور**: Unsplash Source (مجاني، CDN سريع) + grayscale CSS
- **محتوى**: seed data ضخم في migration، الأدمن يقدر يعدل بعدها

---

## ما ستحتاجه مني بعد الموافقة

1. **API key لـ ElevenLabs** — هتدخله في نافذة آمنة لما أطلبه (مجاني للتجربة على elevenlabs.io)
2. **بيانات حساب الأدمن** — هتسجل دخول بعد ما أنشئ النظام، وأنا هضيف دور admin لإيميلك في DB

---

موافق نبدأ بالمرحلة 1؟
